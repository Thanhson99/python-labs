# Foundation Milestone 1 - CLI Data Toolkit

Build a command-line tool that:
- loads JSON records
- filters by status
- exports summary report

Run:
```bash
python3 app.py --input sample.json --status active
```
